<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ZWave.Controls.ShadowContainer"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <Border BackgroundColor="White" Stroke="Transparent">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow
                        Brush="{StaticResource Black}"
                        Opacity="0.2"
                        Radius="4"
                        Offset="0,0" />
                </Border.Shadow>
                <ContentPresenter />
            </Border>
        </ControlTemplate>
    </ContentView.ControlTemplate>
</ContentView>
